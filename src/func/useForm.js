import { useState } from "react";

export default function useFrom(){
    const [values, setValues] = useState({ 

    });
    const  handleChange =()=>{
        setValues({ });

    }
    const handleSubmit =()=>{

    }
    let errors;
}